#include "main.h"
#include <unistd.h>
/**
 *_printf - takes in a string and prints different types of arguments for
 * an unspecified amount of arguments
 * @format: the initial string that tell us what is going to be printed
 * Return: the amount of times stdout is written
 */
int _printf(const char *fromat, ...)
{
        
        if(!format){
                return (-1);
              
        unsigned int count;
        int charPrinted = 0;
        int identifierPrinted = 0;
        int count = 0;
        int store = 0;
        int (*identify)(va_list);
        var_list arg;
        var_start(arg, format);
          
        for (count = 0; format[count] != '\0'; count++)
        {
                if (format[count] != '%')
                {
                        _putchar(format[count]);
                        count++;
                        continue;
                }
                if (format[count+1] == '%')
                {
                        if (format[count+1] == '\0')
                                return (-1);
                        identify = identifier(format[count+1])
                        _putchar('%');
                        if (f == NULL)
				store += print_nan(format[count], format[count + 1]);
			else
				store += identifier(arg);
                        count++;
                }
        }
        va_end(arg);
        return (count);
}
